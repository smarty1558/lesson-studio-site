{
  "name": "osum-music-academy",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pages:dev": "npx wrangler pages dev dist --r2=PORTFOLIO_BUCKET --d1=DB"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
